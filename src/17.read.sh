#! /bin/sh
# -p 设置提示信息 ； -t 等待用户输入超时秒，timeout
# read -p "请输入" vars
# read -p "请输入一个w" -t 5 vars

# 条件测试
# test 命令评估一个表达式，他的结果是真，还是假，如果条件为真，那么命令是0, 否则就不为0 通过 $? 取值
# test 命令的参数
#    -e 判断该文件是否存在，存在为真
#    -f 该文件名是否为文件
#    -d 是否为目录

# -eq  数值相等
# -ne  数值不等
# -gt  大于
# -lt  小于
# -ge  大于等于
# -le  小于等于

